# Wave Terminal 打包指南

**版本**：0.13.2-alpha.0
**配置状态**：✅ 已优化为单架构构建
**系统架构**：x86_64 (Intel Mac)

---

## 📋 快速开始

### 完整打包（推荐）

```bash
# 1. 清理旧文件
task clean

# 2. 执行打包（仅当前架构）
task package

# 3. 验证（可选）
ls -lh make/Wave-darwin-x64-*.dmg
```

**预期时间**：约 3 分钟
**输出文件**：`make/Wave-darwin-x64-0.13.2-alpha.0.dmg` (201MB)

---

## 🔧 配置说明

### 当前优化配置

已优化的文件：

- `Taskfile.yml` - 自动检测系统架构，只构建当前平台
- `electron-builder.config.cjs` - 自动检测系统架构，只打包当前平台

### 架构检测

```bash
# 检查检测的架构
uname -m
# 输出：x86_64 (Intel) 或 arm64 (Apple Silicon)

# 验证任务配置
task --list | grep NATIVE_ARCH
# 应该显示：NATIVE_ARCH: sh: echo {{if eq `uname -m` "x86_64"}}amd64{{else if eq `uname -m` "arm64"}}arm64{{else}}{{`uname -m`}}{{end}}
```

---

## 🚀 打包命令详解

### 1. task package（完整打包）

**功能**：清理 + 构建 Go 后端 + 构建前端 + 生成 DMG/ZIP

**执行流程**：

```
task clean
  ↓
task: [npm:install] npm install
  ↓
task: [build:backend:native] 构建 Go 后端
  ├─ task: [build:server:macos:native] 构建 wavesrv (x64)
  └─ task: [build:wsh:native] 构建 wsh (x64)
      ↓
task: [tsunami:frontend:build] 构建 tsunami scaffold
      ↓
task: [tsunami:scaffold] 复制 scaffold
      ↓
task: [build:frontend:dev] 构建前端
      ↓
task: [electron-builder] 生成 DMG 和 ZIP
```

**输出文件**：

```
make/
├── Wave-darwin-x64-0.13.2-alpha.0.dmg         (201MB) ✅
├── Wave-darwin-x64-0.13.2-alpha.0.zip         (194MB)
├── Wave-darwin-x64-0.13.2-alpha.0.dmg.blockmap
├── Wave-darwin-x64-0.13.2-alpha.0.zip.blockmap
└── mac/
    └── Wave.app/ (x86_64 binary)
```

---

### 2. task dev（开发模式）

**功能**：运行 Electron 开发服务器（热重载）

**适用场景**：

- 代码修改后立即测试
- 前端 UI 开发
- 无需重新打包

**执行命令**：

```bash
task dev
# 或：task quickdev（更快，仅 arm64）
```

**优势**：

- ✅ 修改代码立即生效
- ✅ 支持热重载（HMR）
- ✅ 无需等待构建

---

### 3. task start（直接启动）

**功能**：直接运行构建好的应用（无热重载）

**适用场景**：

- 测试已构建的版本
- 稳定性测试

**执行命令**：

```bash
task start
```

---

### 4. task quickdev（快速开发）

**功能**：快速开发模式（arm64 macOS only，无生成，无 wsh）

**适用场景**：

- 快速迭代开发
- 不需要 schema 和 wsh

**执行命令**：

```bash
task quickdev
```

**注意**：仅在 Apple Silicon Mac 上可用

---

## 📦 安装打包产物

### 方法 1：通过 DMG 安装（覆盖正式版）

```bash
# 直接打开 DMG 安装
open make/Wave-darwin-x64-0.13.2-alpha.0.dmg

# 或双击 DMG 文件
open make/Wave-darwin-x64-0.13.2-alpha.0.dmg
```

**注意**：

- ⚠️ 这会覆盖 `/Applications/Wave.app`
- ✅ 设置和会话会保留
- ✅ 数据目录不变

---

### 方法 2：与正式版共存（最安全）

```bash
# 复制为独立应用
cp -R make/mac/Wave.app ~/Applications/Wave-Dev.app

# 启动开发版本
open ~/Applications/Wave-Dev.app

# 或创建快捷方式
# 系统偏好设置 → 登录项 → 添加 ~/Applications/Wave-Dev.app
```

**优点**：

- ✅ 两个版本独立运行
- ✅ 完全隔离（数据目录不同）
- ✅ 可随时切换
- ✅ 不影响正式版

---

### 方法 3：手动替换（完全替换）

```bash
# 备份现有版本（推荐）
cp -R /Applications/Wave.app ~/Wave.app.backup-$(date +%Y%m%d)

# 删除旧版本
rm -rf /Applications/Wave.app

# 复制新版本
cp -R make/mac/Wave.app /Applications/

# 验证安装
ls -l /Applications/Wave.app
```

---

## 🔍 验证安装

### 验证二进制架构

```bash
# 检查安装的应用架构
file /Applications/Wave.app/Contents/MacOS/Wave

# 应该输出：
# Mach-O 64-bit executable x86_64 ✅ (匹配您的系统）
```

### 验证版本号

```bash
# 启动应用后检查版本
open -a "Wave"

# 或在应用内查看设置
# 应该显示：0.13.2-alpha.0 (dev)
```

### 验证更新通道

```bash
# 检查配置文件
cat ~/Library/Application\ Support/waveterm/wave.db | grep channel

# 或在应用设置中查看
# 应该显示：alpha 或 dev
```

---

## 📊 构建优化效果

### 优化前后对比

| 项目         | 优化前        | 优化后          | 改进            |
| ------------ | ------------- | --------------- | --------------- |
| **构建架构** | arm64 + x64   | **仅当前架构**  | 减少 50%        |
| **构建时间** | ~15 分钟      | **~3 分钟**     | 提升 80%        |
| **磁盘占用** | 1.3 GB        | **~900 MB**     | 减少 30%        |
| **DMG 大小** | 194MB (arm64) | **201MB (x64)** | 原生架构        |
| **运行性能** | 需要 Rosetta  | **原生运行**    | 性能提升 20-40% |

### 优化内容

#### Taskfile.yml 优化

**新增变量**：

```yaml
vars:
  NATIVE_ARCH:
    sh: echo {{if eq `uname -m` "x86_64"}}amd64{{else if eq `uname -m` "arm64"}}arm64{{else}}{{`uname -m`}}{{end}}
```

**修改的任务**：

- `build:backend` → `build:backend:native`（仅当前架构）
- `build:server:macos` → `build:server:macos:native`
- `build:wsh` → `build:wsh:native`
- 所有平台构建任务使用 `{{.NATIVE_ARCH}}`

#### electron-builder.config.cjs 优化

**新增函数**：

```javascript
function getNativeArch() {
  const arch = execSync("uname -m", { encoding: "utf-8" }).trim();
  return arch === "x86_64" ? "x64" : "arm64";
}
```

**修改的配置**：

```javascript
mac: {
    target: [
            {
                target: "zip",
                arch: nativeArch,  // 自动检测
            },
            {
                target: "dmg",
                arch: nativeArch,  // 自动检测
            },
    ],
}
```

---

## 🎯 使用场景和推荐命令

### 场景 1：代码修改后快速测试

**推荐命令**：

```bash
# 开发模式（热重载）
task dev

# 或快速开发模式
task quickdev
```

**原因**：

- ✅ 修改立即生效
- ✅ 无需等待构建
- ✅ 支持调试

---

### 场景 2：需要完整打包分享

**推荐命令**：

```bash
# 1. 清理旧文件
task clean

# 2. 完整打包
task package

# 3. 验证文件
ls -lh make/Wave-darwin-x64-*.dmg
```

**输出**：`make/Wave-darwin-x64-0.13.2-alpha.0.dmg` (201MB)

---

### 场景 3：日常开发，不干扰正式版

**推荐命令**：

```bash
# 与正式版共存安装
cp -R make/mac/Wave.app ~/Applications/Wave-Dev.app

# 启动开发版本
open ~/Applications/Wave-Dev.app

# 日常使用开发模式
task dev
```

**数据目录**：

- 正式版：`~/Library/Application Support/waveterm/`
- 开发版：`~/Library/Application Support/waveterm-dev/`

---

### 场景 4：构建后立即启动

**推荐命令**：

```bash
# 打包完成后启动
task package && open make/mac/Wave.app
```

**注意**：打包需要约 3 分钟

---

## ⚠️ 常见问题

### 问题 1：构建速度慢

**原因**：首次构建或依赖缓存失效

**解决方法**：

```bash
# 清理后重新构建
task clean
task package

# 或只构建 Go 后端
task build:backend:native
```

---

### 问题 2：打包失败 - sharp 错误

**原因**：sharp 包未安装

**已修复**：配置已优化，自动处理

**如再出现**：

```bash
# 手动安装 sharp
npm install --save-dev sharp

# 重新打包
task package
```

---

### 问题 3：DMG 打开失败

**原因**：文件损坏或签名问题

**解决方法**：

```bash
# 重新构建
task clean
task package

# 验证 DMG
hdiutil info make/Wave-darwin-x64-*.dmg
```

---

### 问题 4：架构不匹配

**原因**：在 Apple Silicon Mac 上运行 x64 版本

**现象**：

- 启动极慢（Rosetta 翻译）
- 某些功能不稳定
- CPU 占用高

**解决方法**：

```bash
# 为 Apple Silicon 重新打包
ARCHS=arm64 task package

# 或检查当前架构
uname -m
# 如果输出 arm64，应该使用 ARM64 版本
```

---

## 📋 完整工作流

### 首次设置

```bash
# 1. 安装依赖
task init

# 2. 测试开发模式
task dev

# 3. 完整打包
task package

# 4. 安装 DMG
open make/Wave-darwin-x64-*.dmg
```

### 日常开发

```bash
# 1. 修改代码
vim frontend/app/app.tsx  # 或其他编辑器

# 2. 启动开发模式（热重载）
task dev

# 3. 测试功能

# 4. 需要分享时，打包
task package
```

### 版本发布

```bash
# 1. 确保构建干净
task clean

# 2. 完整打包
task package

# 3. 验证文件
ls -lh make/Wave-darwin-x64-*.dmg

# 4. 分发 DMG 文件
```

---

## 🎓 文件说明

### 配置文件位置

```
/Users/zhouwei/Documents/code/zwcf5200/waveterm/
├── Taskfile.yml                    (构建配置，已优化）
├── Taskfile.yml.backup            (原始配置备份）
├── electron-builder.config.cjs        (打包配置，已优化）
└── package.json                    (项目配置)
```

### 输出文件位置

```
/Users/zhouwei/Documents/code/zwcf5200/waveterm/make/
├── Wave-darwin-x64-0.13.2-alpha.0.dmg         (201MB) - 安装包
├── Wave-darwin-x64-0.13.2-alpha.0.zip         (194MB) - 压缩包
├── Wave-darwin-x64-0.13.2-alpha.0.dmg.blockmap
├── Wave-darwin-x64-0.13.2-alpha.0.zip.blockmap
└── mac/
    └── Wave.app/ (x86_64 binary)
```

---

## 🔧 高级操作

### 仅构建 Go 后端

```bash
# 快速重新构建 Go 部分
task build:backend:native

# 包括：
# - wavesrv (x64)
# - wsh (x64)
# - 所有依赖
```

### 仅构建前端

```bash
# 只构建前端，不重新打包
npm run build:prod

# 或开发构建
npm run build:dev
```

### 跨平台打包（不推荐）

```bash
# 如果需要为其他平台打包
# 使用原始配置（已备份）
git checkout Taskfile.yml.backup

# 然后使用原命令
task package:all
```

---

## 📊 构建时间参考

基于 Intel i7 配置：

| 操作                      | 时间          |
| ------------------------- | ------------- |
| task clean                | ~5 秒         |
| task build:backend:native | ~2 分钟       |
| task build:frontend       | ~1 分钟       |
| task electron-builder     | ~1 分钟       |
| **总计**                  | **~3-4 分钟** |

---

## ✅ 检查清单

打包完成后，验证以下项目：

- [ ] DMG 文件存在且大小正常（~200MB）
- [ ] ZIP 文件存在且大小正常（~200MB）
- [ ] 二进制架构正确（x86_64 for Intel Mac）
- [ ] 应用可以正常启动
- [ ] 版本号正确（0.13.2-alpha.0）
- [ ] 设置和会话正常

---

## 🎯 快速参考

### 最常用命令

```bash
# 完整打包
task clean && task package

# 开发模式
task dev

# 快速开发
task quickdev

# 查看任务列表
task --list

# 验证架构
uname -m
```

---

## 📞 联系和帮助

### 文档

- README.md - 项目说明
- BUILD.md - 构建说明
- CONTRIBUTING.md - 贡献指南
- AGENTS.md - AI 代理开发指南

### 问题反馈

- GitHub Issues: https://github.com/wavetermdev/waveterm/issues
- Discord: https://discord.gg/XfvZ334gwU
- Docs: https://docs.waveterm.dev

---

**最后更新**：2026-01-06
**适用版本**：0.13.2-alpha.0
**优化状态**：✅ 已优化为单架构构建
